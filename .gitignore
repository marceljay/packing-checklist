# Secrets — never commit
.env*
*.pem
*.key
credentials.json

.DS_Store
*.DS_Store
Thumbs.db

# Node / build artifacts
node_modules/
dist/
dist-single/
*.local
.vite/
*.tsbuildinfo

# Local-only working dirs: agent/dev tooling and planning artifacts. Kept on
# disk for local use but deliberately out of git (purged from history too).
.claude/
.devcontainer/
.superpowers/
_planning/
BOILERPLATE.md
